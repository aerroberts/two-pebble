{
  "name": "@two-pebble/app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "ci": "bun lint && bun run guard && bun run build && bun run typecheck && bun run test",
    "dev": "vite --host 127.0.0.1 --port 5173",
    "guard": "guardrails",
    "lint": "biome check --write .",
    "test": "echo 'No app tests configured'",
    "test:coverage": "bun test --coverage",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@two-pebble/components": "workspace:*",
    "@two-pebble/datatypes": "workspace:*",
    "@two-pebble/names": "workspace:*",
    "@two-pebble/pebble": "workspace:*",
    "@two-pebble/realtime": "workspace:*",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "react-router-dom": "6.30.1"
  },
  "devDependencies": {
    "@two-pebble/guardrails": "workspace:*",
    "@types/react": "18.3.12",
    "@types/react-dom": "18.3.1",
    "@vitejs/plugin-react": "4.3.4",
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.17",
    "vite": "5.4.21"
  }
}
