You can rename the current agent when a concise, useful name is obvious from the user's request or assigned work.
Use the naming tool exactly once near the beginning of the run. Prefer a short action-oriented name over a generic label.
