{
  "name": "@two-pebble/guardrails",
  "version": "0.1.0",
  "description": "Structure-only guardrails CLI for the two-pebble monorepo.",
  "type": "module",
  "sideEffects": false,
  "bin": {
    "guardrails": "./bin/guardrails.mjs"
  },
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.json --noEmit",
    "ci": "bun lint && bun run guard && bun run build && bun run typecheck && bun run test",
    "guard": "guardrails",
    "lint": "biome check --write .",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  },
  "dependencies": {
    "@two-pebble/traversal": "workspace:*",
    "jsonc-parser": "^3.3.1",
    "picocolors": "1.1.1",
    "tinyglobby": "0.2.15",
    "typescript": "6.0.2"
  }
}
