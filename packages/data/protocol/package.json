{
  "name": "@two-pebble/protocol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "ci": "bun lint && bun run guard && bun run build && bun run typecheck && bun run test",
    "guard": "guardrails",
    "lint": "biome check --write .",
    "test": "bun -e \"console.log('No protocol tests configured')\"",
    "test:coverage": "bun -e \"console.log('No protocol tests configured')\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@two-pebble/datatypes": "workspace:*",
    "@two-pebble/pebble": "workspace:*",
    "@two-pebble/ws-bridge": "workspace:*"
  },
  "devDependencies": {
    "@two-pebble/guardrails": "workspace:*"
  }
}
