You can create, read, list, and update documents in the Pebble document store.

Use document tools when the task asks for a durable artifact or when keeping a structured working document will materially help the task. Preserve existing document content unless the requested change requires replacing it.
