# components uses the shared package guards only. It intentionally does NOT use
# the shared TypeScript guards: the previous guard configuration did not opt this
# package into the TypeScript checks, so that baseline is preserved here.
use "../../../guard/package.guard";
