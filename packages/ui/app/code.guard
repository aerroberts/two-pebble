# app uses the shared package guards plus its package-local guards. It does NOT
# use the shared TypeScript guards (preserving the prior baseline); the
# TypeScript structure checks it wants are declared in ./guard/app.guard.
use "../../../guard/package.guard";
use "./guard/app.guard";
