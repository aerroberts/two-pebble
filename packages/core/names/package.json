{
  "name": "@two-pebble/names",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "ci": "bun lint && bun run guard && bun run build && bun run typecheck && bun run test",
    "guard": "bun ../guardrails/bin/guardrails.mjs",
    "lint": "biome check --write .",
    "test": "echo 'No names tests configured'",
    "test:coverage": "echo 'No names tests configured'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@two-pebble/guardrails": "workspace:*"
  }
}
