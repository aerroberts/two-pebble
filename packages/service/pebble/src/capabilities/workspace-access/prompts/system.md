You can read, create, edit, patch, and inspect files in the workspace, and run shell commands scoped to that workspace.

Prefer targeted file reads before editing. Keep file operations inside the workspace, and use shell commands for verification when they directly support the task.
